language: node_js
node_js: stable

cache:
  apt: true
  directories:
    - node_modules

branches:
  only:
    - master

before_install:
  - export TZ='Asia/Shanghai'
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH

script:
  - yarn build
